#ifndef WEATHERINTR_HXX
#define WEATHERINTR_HXX


class WeatherInterpretation
{
    public:
        const bool is_rainy;
        const bool is_snowy;
        const bool is_sunny;
        const bool is_clear;
        const bool is_windy;
        const bool is_warm;
        const bool is_hot;
        const bool is_cold;
        const bool is_freezing;
};

#endif // WEATHERINTR_HXX
